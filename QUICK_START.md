# 🚀 QUICK START - БЫСТРЫЙ СТАРТ ИНТЕГРАЦИИ

Этот файл содержит минимум информации для быстрого внедрения системы презентаций.

## ⏱️ Время на интеграцию: ~15 минут

## 📋 Шаги интеграции

### 1. Применить миграцию БД (1 минута)

```bash
cd supabase
supabase db push
```

Это создаст две новые таблицы:

- `presentations` - метаданные PDF
- `presentation_sections` - разделы презентации

### 2. Интеграция в OrganizerDashboard (~5 минут)

**Файл**: `src/pages/OrganizerDashboard.tsx`

**Добавить импорт** (в самом начале импортов):

```tsx
import { OrganizerPresentations } from "../components/organizer";
```

**Найти строку ~1750** где есть:

```tsx
{selectedWedding && viewMode === 'wedding-details' && (
  // ... существующий контент
)}
```

**Добавить в конец существующего блока**:

```tsx
{
  selectedWedding && viewMode === "wedding-details" && (
    <div className="p-6 bg-white rounded-lg border border-gray-200">
      <OrganizerPresentations weddingId={selectedWedding.id} />
    </div>
  );
}
```

Готово! ✅

### 3. Интеграция в ClientDashboard (~5 минут)

**Файл**: `src/pages/ClientDashboard.tsx`

**Добавить импорты** (в самом начале):

```tsx
import PresentationViewer from "../components/PresentationViewer";
import { useCustomPresentations } from "../hooks";
```

**Найти строку ~30** где есть другие hooks:

```tsx
const {
  data: wedding,
  isLoading: weddingLoading,
  error: weddingError,
} = useClientWedding(user?.id);
const { data: tasks = [] } = useWeddingTasks(wedding?.id);
const { data: documents = [] } = useWeddingDocuments(wedding?.id);
```

**Добавить после них**:

```tsx
const { data: customPresentations = [] } = useCustomPresentations(wedding?.id);
```

**Найти строку ~437** где есть:

```tsx
{
  wedding && (
    <Presentation
      presentation={wedding.presentation}
      currentLanguage={currentLanguage}
    />
  );
}
```

**Заменить на**:

```tsx
{
  /* Новые PDF презентации */
}
{
  customPresentations && customPresentations.length > 0 && (
    <PresentationViewer
      presentations={customPresentations}
      currentLanguage={currentLanguage}
    />
  );
}

{
  /* Старая презентация компании (обратная совместимость) */
}
{
  (!customPresentations || customPresentations.length === 0) && wedding && (
    <Presentation
      presentation={wedding.presentation}
      currentLanguage={currentLanguage}
    />
  );
}
```

Готово! ✅

### 4. Тестирование (3 минуты)

**Тест загрузки**:

1. Откройте OrganizerDashboard
2. Выберите любую свадьбу
3. Найдите блок "Презентации"
4. Нажмите "+ Загрузить"
5. Выберите PDF файл
6. Добавьте несколько разделов (название + номер страницы)
7. Нажмите "Загрузить"

**Тест просмотра**:

1. Авторизуйтесь как клиент (свадьба, для которой загружена презентация)
2. Откройте ClientDashboard
3. Прокрутите до "Презентации"
4. Должна появиться загруженная презентация
5. Кликните на раздел в левой панели
6. PDF должен прелистаться на нужную страницу

## 📦 Что уже создано

Все файлы уже созданы и готовы:

| Файл                                                  | Статус                            |
| ----------------------------------------------------- | --------------------------------- |
| `supabase/migrations/044_presentations_refactor.sql`  | ✅ Готов                          |
| `src/types/index.ts`                                  | ✅ Обновлен                       |
| `src/services/weddingService.ts`                      | ✅ Обновлен (presentationService) |
| `src/components/PresentationViewer.tsx`               | ✅ Готов                          |
| `src/components/modals/PresentationUploadModal.tsx`   | ✅ Готов                          |
| `src/components/organizer/OrganizerPresentations.tsx` | ✅ Готов                          |
| `src/hooks/useCustomPresentations.ts`                 | ✅ Готов                          |

Вам нужно только добавить **~11 строк кода** в две страницы!

## 🎯 Итого

- ✅ Загрузка PDF организатором
- ✅ Создание разделов с номерами страниц
- ✅ Просмотр в аккордеоне
- ✅ Хранение в Supabase Storage
- ✅ Показ конкретному пользователю
- ✅ Обратная совместимость со старой системой

## 💡 Примеры

### Как загрузить презентацию (организатор)

1. Нажимаете "+ Загрузить"
2. Заполняете форму:
   ```
   Название: Ольга Португалия
   PDF: выбираете файл
   Разделы:
   - Концепция (страница 3)
   - Стиль (страница 5)
   - Еда (страница 8)
   ```
3. Нажимаете "Загрузить"
4. Готово! Клиенты видят новую презентацию

### Как просмотреть (клиент)

1. Открываете ClientDashboard
2. Видите список презентаций в левой панели (в аккордеоне)
3. Кликаете на "Концепция"
4. PDF показывает страницу 3
5. Кликаете на "Еда"
6. PDF показывает страницу 8

## 🔗 Полная документация

- **PRESENTATION_SYSTEM.md** - подробная техническая документация
- **IMPLEMENTATION_GUIDE.md** - полное руководство с примерами
- **IMPLEMENTATION_REPORT.md** - полный отчет о реализации

## ❓ Вопросы?

Если что-то непонятно:

1. Смотрите IMPLEMENTATION_GUIDE.md (полное руководство)
2. Смотрите PRESENTATION_SYSTEM.md (техническая документация)
3. Проверьте примеры кода в компонентах

## ✨ Дополнительные команды

Если нужно откатить миграцию:

```bash
cd supabase
supabase migration list  # посмотреть список
supabase db reset         # откатить всё
supabase db push          # заново применить все
```

---

**Всё готово к использованию!** 🎉

На интеграцию уходит ~15 минут, на тестирование ~10 минут.

После этого система полностью работает.

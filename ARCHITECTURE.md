# 🏗️ АРХИТЕКТУРА СИСТЕМЫ ПРЕЗЕНТАЦИЙ

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────────┐
│                      ОРГАНИЗАТОР (OrganizerDashboard)           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ OrganizerDashboard                                       │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ viewMode: 'wedding-details'                        │   │   │
│  │ │ selectedWedding: { id, ... }                       │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  │         ↓                                                  │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ OrganizerPresentations (вкладка)                  │   │   │
│  │ │ + Загрузить [кнопка]                              │   │   │
│  │ │ [Список презентаций]                              │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  │         ↓                                                  │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ PresentationUploadModal                            │   │   │
│  │ │ [Форма заполнения данных]                         │   │   │
│  │ │ - Название презентации                            │   │   │
│  │ │ - Выбор PDF файла                                 │   │   │
│  │ │ - Добавление разделов                            │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────────────────────────┐
│              presentationService (API Функции)                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  uploadPresentationPDF(weddingId, file)                          │
│         ↓ загружает в Storage                                    │
│         ↓ возвращает signed URL                                 │
│         ↓                                                        │
│  createPresentation(weddingId, title, pdfUrl, sections)         │
│         ↓ создает запись в БД                                   │
│         ↓ создает разделы (presentation_sections)               │
│         ↓ инвалидирует кеш                                      │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────────────────────────┐
│              SUPABASE (Backend)                                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────────────┐                                │
│  │ PostgreSQL Database         │                                │
│  │                             │                                │
│  │ presentations               │                                │
│  │ ├─ id                       │                                │
│  │ ├─ wedding_id ──────────┐   │                                │
│  │ ├─ title                │   │                                │
│  │ ├─ pdf_url              │   │                                │
│  │ ├─ pdf_file_path        │   │                                │
│  │ ├─ order_index          │   │  ┌──────────────────────┐    │
│  │ └─ ...                  │   │  │ weddings             │    │
│  │                         │   └──├─ id                  │    │
│  │ presentation_sections   │      ├─ client_id          │    │
│  │ ├─ id                   │      ├─ organizer_id       │    │
│  │ ├─ presentation_id ─────┘      └─ ...                │    │
│  │ ├─ title                       └──────────────────────┘    │
│  │ ├─ page_number                                             │
│  │ └─ order_index                                             │
│  └─────────────────────────────┘                                │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Storage Bucket: wedding-documents                        │  │
│  │                                                          │  │
│  │ presentations/                                          │  │
│  │ └─ {wedding_id}/                                       │  │
│  │    └─ {timestamp}_{filename}.pdf                       │  │
│  │       (100MB max, signed URLs 1 year)                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ RLS Политики (безопасность)                             │  │
│  │                                                          │  │
│  │ presentations:                                           │  │
│  │ - Клиенты видят свои свадьбы                           │  │
│  │ - Организаторы видят свои                              │  │
│  │ - Главный видит все                                    │  │
│  │                                                          │  │
│  │ presentation_sections:                                  │  │
│  │ - Наследуют логику presentations                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
           ↓ (get presentations)
┌──────────────────────────────────────────────────────────────────┐
│                      КЛИЕНТ (ClientDashboard)                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ ClientDashboard                                          │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ useCustomPresentations(weddingId)                 │   │   │
│  │ │ (React Query hook)                                 │   │   │
│  │ │ - кеширует 5 минут                                │   │   │
│  │ │ - возвращает массив presentations                 │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  │         ↓                                                  │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ PresentationViewer (компонент)                    │   │   │
│  │ │                                                    │   │   │
│  │ │ ┌──────────────┐  ┌─────────────────────────┐   │   │   │
│  │ │ │ Левая панель │  │ Правая панель           │   │   │   │
│  │ │ │              │  │                         │   │   │   │
│  │ │ │ Презентация  │  │ iframe (PDF viewer)     │   │   │   │
│  │ │ │ 1 Ольга      │  │                         │   │   │   │
│  │ │ │ ├─Концепция  │→ │ Показывает PDF          │   │   │   │
│  │ │ │ │  (стр. 3)  │  │ на странице 3           │   │   │   │
│  │ │ │ ├─Стиль      │→ │ Показывает PDF          │   │   │   │
│  │ │ │ │  (стр. 5)  │  │ на странице 5           │   │   │   │
│  │ │ │ └─Еда        │→ │ Показывает PDF          │   │   │   │
│  │ │ │    (стр. 8)  │  │ на странице 8           │   │   │   │
│  │ │ │              │  │                         │   │   │   │
│  │ │ │ Презентация  │  │                         │   │   │   │
│  │ │ │ 2 Марина     │  │                         │   │   │   │
│  │ │ │ ├─...        │  │                         │   │   │   │
│  │ │ │ └─...        │  │                         │   │   │   │
│  │ │ └──────────────┘  └─────────────────────────┘   │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  │ Если нет новых презентаций, показывает:                │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ Presentation (старая система - изображения)       │   │   │
│  │ │ Обратная совместимость                            │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
```

## Схема таблиц БД

```
weddings (существующая)
│
├─ id (UUID PRIMARY KEY)
├─ client_id (связь с users)
├─ organizer_id (связь с users)
├─ couple_name_1_en
├─ couple_name_2_en
├─ wedding_date
├─ ...
└─ (RLS: клиенты видят свои, организаторы - свои)

                ↓ (FK: wedding_id)

presentations (новая)
│
├─ id (UUID PRIMARY KEY)
├─ wedding_id (FK → weddings.id)
├─ title (TEXT)
├─ type ('company' | 'wedding')
├─ pdf_url (TEXT, signed URL)
├─ pdf_file_path (TEXT, путь в Storage)
├─ pdf_file_size (BIGINT)
├─ is_default (BOOLEAN)
├─ order_index (INTEGER)
├─ created_at (TIMESTAMP)
├─ updated_at (TIMESTAMP)
└─ (RLS: видят как weddings)

        ↓ (FK: presentation_id)

presentation_sections (новая)
│
├─ id (UUID PRIMARY KEY)
├─ presentation_id (FK → presentations.id)
├─ title (TEXT) - "Концепция"
├─ page_number (INTEGER) - номер страницы
├─ order_index (INTEGER) - порядок в презентации
├─ created_at (TIMESTAMP)
├─ updated_at (TIMESTAMP)
└─ (RLS: видят как presentations)
```

## Поток данных при загрузке

```
1. Пользователь нажимает "+ Загрузить"
   ↓
2. Открывается PresentationUploadModal
   ↓
3. Заполняет форму:
   - Название: "Ольга Португалия"
   - PDF файл: выбирает file.pdf (10MB)
   - Разделы:
     * "Концепция" → страница 3
     * "Стиль" → страница 5
   ↓
4. Нажимает "Загрузить"
   ↓
5. Вызывается handleUploadPresentation()
   ├─ presentationService.uploadPresentationPDF()
   │  └─ POST в Supabase Storage
   │     └─ presentations/wedding-uuid/1704067200000_file.pdf
   │        └─ Получает signed URL
   │
   ├─ presentationService.createPresentation()
   │  └─ INSERT в presentations (одна запись)
   │     {
   │       wedding_id: "wedding-uuid",
   │       title: "Ольга Португалия",
   │       pdf_url: "https://...signed-url...",
   │       pdf_file_path: "presentations/wedding-uuid/...",
   │       is_default: false,
   │       order_index: 1
   │     }
   │  └─ INSERT в presentation_sections (две записи)
   │     {presentation_id: "...", title: "Концепция", page_number: 3}
   │     {presentation_id: "...", title: "Стиль", page_number: 5}
   │
   └─ Инвалидирует кеш
      └─ React Query обновляет useCustomPresentations()

6. Клиент видит в ClientDashboard:
   ├─ "Ольга Португалия" в левой панели
   │  ├─ Концепция (стр. 3)
   │  └─ Стиль (стр. 5)
   └─ PDF в правой панели
```

## Поток данных при просмотре

```
1. Клиент открывает ClientDashboard
   ↓
2. Компонент вызывает useCustomPresentations(weddingId)
   ├─ React Query выполняет SELECT из presentations/sections
   ├─ Результат кешируется на 5 минут
   └─ Возвращает массив presentations с вложенными sections

3. PresentationViewer получает массив presentations
   ├─ Рендерит левую панель с аккордеоном
   │  └─ Каждая презентация → список разделов
   │
   └─ Рендерит правую панель с iframe
      └─ <iframe src={pdf_url}#page={currentPage} />

4. Клиент кликает на раздел "Концепция" (page_number: 3)
   ├─ setCurrentPage(3)
   └─ iframe переходит на страницу 3
      └─ URL становится: https://...#page=3

5. Клиент кликает на раздел "Стиль" (page_number: 5)
   ├─ setCurrentPage(5)
   └─ iframe переходит на страницу 5
      └─ URL становится: https://...#page=5
```

## Компоненты и их взаимодействие

```
OrganizerDashboard (страница)
│
├─ viewMode: 'wedding-details'
│  └─ OrganizerPresentations (компонент управления)
│     ├─ useCustomPresentations (hook) → fetch presentations
│     ├─ [Список загруженных]
│     │  └─ handleDeletePresentation()
│     │     └─ presentationService.deletePresentation()
│     │
│     └─ PresentationUploadModal (модал)
│        └─ handleUploadPresentation()
│           ├─ presentationService.uploadPresentationPDF()
│           └─ presentationService.createPresentation()
│


ClientDashboard (страница)
│
├─ useCustomPresentations (hook) → fetch presentations
│  └─ refetch при изменении weddingId
│
└─ conditional render:
   ├─ if (customPresentations.length > 0)
   │  └─ PresentationViewer (компонент просмотра)
   │     ├─ Левая панель (аккордеон)
   │     └─ Правая панель (iframe)
   │
   └─ else
      └─ Presentation (старая система - обратная совместимость)
```

## Файловая структура хранилища

```
Supabase Storage (бакет: wedding-documents)
│
└─ presentations/
   │
   ├─ {wedding_id_1}/
   │  ├─ 1704067200000_presentation_1.pdf (10MB)
   │  ├─ 1704067300000_presentation_2.pdf (15MB)
   │  └─ 1704067400000_presentation_3.pdf (8MB)
   │
   ├─ {wedding_id_2}/
   │  └─ 1704067500000_my_presentation.pdf (12MB)
   │
   └─ {wedding_id_3}/
      └─ ...
```

## RLS Политики (безопасность)

```
presentations таблица:
│
├─ SELECT (Клиент может видеть)
│  └─ wedding_id IN (SELECT id FROM weddings WHERE client_id = current_user_id)
│
├─ SELECT (Организатор может видеть)
│  └─ true (видит все)
│
├─ INSERT/UPDATE/DELETE (Организатор может управлять)
│  └─ wedding_id IN (SELECT id FROM weddings WHERE organizer_id = current_user_id)
│
└─ INSERT/UPDATE/DELETE (Главный организатор может управлять)
   └─ current_user_id IN (SELECT id FROM auth.users WHERE role = 'main_organizer')

presentation_sections таблица:
│
└─ наследует логику presentations
   (видит/управляет через presentation_id)
```

## Пример реальных данных

```json
// Запрос: SELECT * FROM presentations WHERE wedding_id = 'abc123'
[
  {
    "id": "pres_001",
    "wedding_id": "abc123",
    "title": "Ольга Португалия",
    "type": "wedding",
    "pdf_url": "https://...signed-url...#page=",
    "pdf_file_path": "presentations/abc123/1704067200000_portfolio.pdf",
    "pdf_file_size": 10485760,
    "is_default": false,
    "order_index": 1,
    "created_at": "2024-01-01T12:00:00Z",
    "presentation_sections": [
      {
        "id": "sect_001",
        "presentation_id": "pres_001",
        "title": "Концепция",
        "page_number": 3,
        "order_index": 0
      },
      {
        "id": "sect_002",
        "presentation_id": "pres_001",
        "title": "Стиль",
        "page_number": 5,
        "order_index": 1
      }
    ]
  },
  {
    "id": "pres_002",
    "wedding_id": "abc123",
    "title": "Марина и Игорь",
    "type": "wedding",
    "pdf_url": "https://...signed-url...#page=",
    "pdf_file_path": "presentations/abc123/1704067300000_wedding.pdf",
    "pdf_file_size": 15728640,
    "is_default": false,
    "order_index": 2,
    "presentation_sections": [
      {
        "id": "sect_003",
        "presentation_id": "pres_002",
        "title": "Сценарий",
        "page_number": 1,
        "order_index": 0
      }
    ]
  }
]
```

---

Эта диаграмма показывает полный поток данных от загрузки до просмотра!
